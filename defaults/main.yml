---
# defaults file for php-fpm

# These are examples which should be put in inventory group_vars, host_vars or
# inline vars

# php_origin: 'default' (Debian repositories) or 'sury' (deb.sury.org)
php_origin: 'default'

# php_version: "7.4"

# fpm_pools: define variables for specific pool types
# fpm_pools:
#   - name: 'mypool'
#     listen: "/run/php/php{{ php_version }}-fpm-mypool.sock"
#     listen_owner: www-data
#     listen_group: www-data
#     type: static
#     max_children: 10
#   - name: 'yourpool'
#     listen: "/run/php/php{{ php_version }}-fpm-yourpool.sock"
#     listen_owner: www-data
#     listen_group: www-data
#     type: dynamic
#     max_children: 5
#     start_servers: 2
#     min_spare_servers: 1
#     max_spare_servers: 3
#   - name: 'theirpool'
#     listen: "/run/php/php{{ php_version }}-fpm-theirpool.sock"
#     listen_owner: www-data
#     listen_group: www-data
#     type: ondemand
#     max_children: 120
#     process_idle_timeout: 120
#     memory_limit: 128M
