---
# defaults file for php-fpm

# These are examples which should be put in inventory group_vars, host_vars or
# inline vars

# php_origin: 'default' (Debian packages : 7.0) or 'sury' (7.[0123])
php_origin: 'default'

# php_version: "7.0"

# fpm_pools: define variable for specific pools
# fpm_pools:
#   - name: 'mypool'
#     max_children: '10'
#   - name: 'yourpool'
#     max_children: '15'
...
